[project]
name = "pytorch"
version = "0.1.0"
description = "pytorch learning"
readme = "README.md"
authors = [
    { name = "tarun gupta", email = "taruneng12@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pytorch = "pytorch:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
